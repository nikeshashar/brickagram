<main class='container'>
  <% if @posts.any? %>
    <section class='row'>
      <%= render @posts %>
     </section>
  <% else %>
    There are no posts
  <% end %>

  <%= link_to new_post_path, class: 'btn btn-lg btn-success' do %>
  New Post <span class="glyphicon glyphicon-pencil"></span>
    <% end %>
</main>